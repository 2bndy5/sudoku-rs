[package]
name = "sudoku-game"
version = "0.1.0"
edition.workspace = true
repository.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
bevy = { version = "0.19.0", default-features = false, features = ["ui"] }
sudoku-gen = { path = "../generator", version = "0.1.0" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
