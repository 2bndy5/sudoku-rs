# sudoku-rs

A sudoku project written in rust.

This repository is a cargo workspace (aka "monorepo").
The key components of this project are:

- `sudoku-gen` (in path [crates/generator](crates/generator))
  A library to generate sudoku puzzles.
